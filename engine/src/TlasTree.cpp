#include "engine/util/TlasTree.hpp"
#include "engine/resources/RayTracingModel.hpp"

namespace engine::util::ds {

std::vector<TlasTree::TlasNode> TlasTree::build_tlas(std::vector<resources::RayTracingModel> &rt_models) {
    for (auto &r: rt_models) {
    }
}
}// namespace engine::util::ds
